# Antigravity Setup - macOS 开发环境一键安装工具

这是一个基于 Electron + Vite + Vue 3 构建的 macOS 桌面工具，旨在帮助开发者一键配置开发环境。

## 🚀 快速开始

### 1. 前置要求
- **macOS** 系统 (Intel 或 Apple Silicon)
- **Node.js** (推荐 v18+)
- **npm** 或 **pnpm**

### 2. 安装依赖
在项目根目录下执行：
```bash
npm install
```

### 3. 启动开发模式
执行以下命令将同时启动 Vite 开发服务器和 Electron：
```bash
npm run electron:dev
```

### 4. 项目打包
构建生产环境安装包：
```bash
npm run build
```

## 🛠 技术栈
- **核心**: Electron (主进程)
- **前端**: Vue 3 + Vite + TypeScript
- **状态管理**: Pinia
- **样式**: Tailwind CSS + Element Plus
- **系统交互**: Node.js `spawn` 实现流式日志传输

## 📂 项目结构
- `packages/main`: 主进程核心逻辑、安装服务、软件配置。
- `packages/preload`: 用于 IPC 通信的安全桥接脚本。
- `packages/renderer`: Vue 3 实现的 UI 界面。
- `dist`: 编译后的产物目录。

## 📝 软件配置
您可以直接在 `packages/main/config.ts` 中修改 `SOFTWARE_LIST` 来增删需要安装的工具。

---
**由 Antigravity 设计与驱动。**
# dev-env-installer
